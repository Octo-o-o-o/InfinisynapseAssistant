# Upstream Source Placeholder

The official private deployment guide references:

```bash
git clone https://github.com/chaozwn/infini_docker.git
```

As of 2026-06-23, that repository returns 404 from local shell checks. When it becomes available, clone it here:

```bash
git clone https://github.com/chaozwn/infini_docker.git upstream-src/infini_docker
```

If you receive an offline delivery archive, unpack it to:

```text
upstream-src/infini_docker/
```

Then update `docs/SOURCE-AUDIT.md`.

